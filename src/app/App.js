import {
  default as React,
  Component,
} from "react";

import {
  Application,
} from "./containers";

export default class App extends Component {
  render() {
    return (
     <Application />
    );
  }
}
